

```cpp
int func1(ll a) {
    if (a < 2) return 1; // 0和1不是质数
    if (a == 2) return 0; //2是素数
    if (a % 2 == 0) return 1;//排除偶数
    for (ll i = 3; i * i <= a; i += 2) {//排除3、5、7、、、直到a的开方
        if (a % i == 0) return 1;
    }
    return 0;
}
```