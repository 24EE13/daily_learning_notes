# STL之Priority_queue容器   

**priority_queue**优先队列可以在放入元素时进行快速排序，他是队列的加强版
底层逻辑是二叉堆（*我还没学（*）


## 定义优先队列

`priority_queue<类型,容器，比较器> pque`

## 队列的相关函数

### 进
`pque.push(元素)`

### 出
`pque.pop()`

### 取顶
`pque.top()`

注：优先队列只能取顶，并且同样不能更改任何元素的值




